# Port the app listens on (host and container).
PORT=3000

# Public origin the site is served from. In your homelab set this to the
# externally reachable URL (behind your reverse proxy), e.g.:
#   ORIGIN=https://whatyaget.example.com
ORIGIN=http://localhost:3000
